/* Copy to wifi_secrets.h and fill in. That file is gitignored: the probe only
 * needs an access point for step 3 of the sequence (join, then leave), which is
 * the one that reproduces the section 23.7 path. Steps 1 and 2 never associate
 * and the values are unused. */
#define WIFI_SSID "your-ssid"
#define WIFI_PASS "your-password"
