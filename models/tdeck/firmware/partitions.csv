# Name,   Type, SubType, Offset,   Size
# NVS at the standard 0x9000 so the callsign, the identity and the WiFi
# credentials survive a reflash, exactly as on the other boards.
nvs,      data, nvs,     0x9000,   0x6000
phy_init, data, phy,     0xf000,   0x1000
# 2MB, not the M5Stack's 1.5MB: the same firmware is already at 83% there, and
# the S3's code is bigger than the original ESP32's for the same source.
factory,  app,  factory, 0x10000,  0x200000
# Everything the indexer stores: a wear-levelled FAT filling the rest of the
# 16MB flash.
storage,  data, fat,     0x210000, 0xD70000
# Where a panic writes its last words: task, PC, backtrace. Read at the next
# boot and put into the rotating log, so a freeze names its culprit.
coredump, data, coredump, 0xF80000, 0x10000
# The 448KB nobody was using. The chip ends at 0x1000000 and coredump ended at
# 0xF90000, so everything below is APPENDED: no offset above moves, which is
# the whole point -- the 11MB FAT archive is not touched by adding these.
#
# Raw, not FAT, and that is deliberate. esp_partition_read works before any
# filesystem is mounted, so the splash draws without FatFs (4-8KB of heap, per
# docs/esp32.md) and without caring about mount order. Same trick as the
# T-Dongle's raw `logs` partition.
assets,   data, 0x40,     0xF90000, 0x30000
# A and B, so a bad script bundle reverts to the last good one instead of
# taking the panels down. Laid out now, used when the script host lands, so
# the partition table is changed once and not twice.
script_a, data, 0x41,     0xFC0000, 0x20000
script_b, data, 0x41,     0xFE0000, 0x20000
