# Name,   Type, SubType,  Offset,   Size
# Two app slots for over-the-air updates (XPRS.md 25.8). No `factory`: see
# the m5stack table for the argument -- rollback beats a stale golden image,
# and the watchdog is what triggers it.
#
# NVS keeps its address and size, so the RNS identity, the callsign and the
# WiFi credentials survive the one cable visit. Never erase_flash.
nvs,      data, nvs,      0x9000,   0x6000
otadata,  data, ota,      0xf000,   0x2000
phy_init, data, phy,      0x11000,  0x1000
# 2 MB per slot; the image is about 1.41 MB, so ~33% spare. This board has
# 16 MB and stores everything else on the SD card, so the slots cost nothing.
ota_0,    app,  ota_0,    0x20000,  0x200000
ota_1,    app,  ota_1,    0x220000, 0x200000
# New here: until now a panic on this board left nothing at all behind, which
# is the single biggest reason a roof node was undiagnosable.
coredump, data, coredump, 0x420000, 0x10000
# Reserved for the flash-backed log ring (phase 2). Raw, not FAT: mounting
# FATFS costs 4-8 KB of heap and this board runs at about 13 KB free.
logs,     data, 0x40,     0x430000, 0x80000
